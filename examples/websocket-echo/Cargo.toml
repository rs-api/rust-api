[package]
name = "websocket-echo"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
foton = { path = "../..", features = ["websocket"] }
tokio = { version = "1", features = ["full"] }
